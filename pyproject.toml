[project]
name = "accra-language"
version = "0.1.0"
description = "Add your description here"
readme = "README"
authors = [
    { name = "Rumen", email = "rumenmitov@disroot.org" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
accra-language = "accra_language:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
